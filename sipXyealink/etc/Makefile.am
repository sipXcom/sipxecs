include $(top_srcdir)/config/utility.am

yealinkdir = $(SIPX_CONFDIR)/yealink
dist_yealink_DATA = $(shell cd $(srcdir); find yealink -type f)

commonvmdir = $(SIPX_CONFDIR)/yealink/common-files
dist_commonvm_DATA = $(shell cd $(srcdir); find yealink/common-files -type f)
